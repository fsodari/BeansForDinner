version: "3"

services:
    web:
        env_file:
            - prod_env.env
        build: .
        container_name: BeansForDinner
        restart: always
        ports:
            - 5000:80
        volumes:
            - .:/BeansForDinner
networks:
    default:
        external:
            name: npm-network
